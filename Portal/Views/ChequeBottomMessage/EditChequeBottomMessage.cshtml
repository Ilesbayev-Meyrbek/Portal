@model Portal.Models.ChequeBottomMessage

@{
    ViewBag.Title = "Изменение пользователя";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    <form asp-action="EditChequeBottomMessage" asp-controller="ChequeBottomMessage">

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            <h2>Изменение сообщение</h2>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Message, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Message, new { htmlAttributes = new {@class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Message, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-actions no-color">
            <p>
                <div class="btnPosition">
                    <input type="submit" value="Сохранить" class="my_btn my_btn_success" />
                </div>

                <div class="btnPositionCancel">
                    @Html.ActionLink("Отмена","ChequeBottomMessages","ChequeBottomMessage",null, new { @class="my_btn my_btn_danger"})
                </div>
            </p>
        </div>

    </form>
}